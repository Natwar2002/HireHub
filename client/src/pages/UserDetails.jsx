export const UserDetails = () => {

    function handleFormSubmit(e) {
        e.preventDefault();
    }

    return (
        <div>
            <form onSubmit={handleFormSubmit}>
                
            </form>
        </div>
    )
}